<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Node.js 🚀</title>
    <style>
      body {
        background: #282c34;
        min-height: 100vh;
        margin: 0;
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: "Segoe UI", Arial, sans-serif;
      }
      .container {
        background: #fff;
        border-radius: 16px;
        box-shadow: 0 4px 24px rgba(40, 44, 52, 0.2);
        padding: 40px 60px;
        display: flex;
        flex-direction: column;
        align-items: center;
        animation: fadeIn 1.5s;
      }
      .logo {
        height: 90px;
        margin-bottom: 24px;
        display: block;
      }
      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
      h1 {
        color: #282c34;
        font-size: 2.2rem;
        margin-bottom: 12px;
        letter-spacing: 1px;
      }
      .server-url {
        color: red;
        font-size: 1.3rem;
        margin-top: 10px;
        text-decoration: none;
        transition: color 0.2s;
      }
      .server-url:hover {
        color: #21a1f3;
        text-decoration: underline;
      }
      .footer {
        margin-top: 32px;
        color: #888;
        font-size: 0.95rem;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <img class="logo" src="/expressjs-icon.svg" alt="Express Logo" />
      <h1>Welcome to Node.js 🚀 !</h1>
      <div>Your server is running at:</div>
      <a class="server-url" href="http://localhost:<%= port %>/"
        >http://localhost:<%= port %>/</a
      >
    </div>
  </body>
</html>
